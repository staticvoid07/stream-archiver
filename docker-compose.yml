services:
  archiver:
    build: .
    ports:
      - "8443:8443"
    volumes:
      - ./data:/data
      - ./config:/config
    environment:
      - DATA_DIR=/data
      - CONFIG_DIR=/config
      - PORT=8443
      - NODE_ENV=production
    restart: unless-stopped
