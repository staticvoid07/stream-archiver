services:
  archiver:
    build: .
    ports:
      - "443:443"
    volumes:
      - ./data:/data
      - ./config:/config
    environment:
      - DATA_DIR=/data
      - CONFIG_DIR=/config
      - PORT=443
      - NODE_ENV=production
    restart: unless-stopped
